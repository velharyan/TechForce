@* @inject Microsoft.Extensions.Options.IOptions<AppOptions> App

<style>
    /* Estilos Específicos da Home - Premium UI */
    .nx-section { padding: 120px 0; position: relative; }
    .nx-container { max-width: 1280px; margin: 0 auto; padding: 0 24px; z-index: 2; position: relative; }
    
    /* Hero Section */
    .nx-hero {
        min-height: 90vh;
        display: flex;
        align-items: center;
        background: radial-gradient(circle at 80% 20%, rgba(56, 189, 248, 0.08) 0%, transparent 50%);
        padding-top: 140px; /* Compensa o header */
    }
    .hero-grid { display: grid; grid-template-columns: 1.1fr 0.9fr; gap: 60px; align-items: center; }
    
    .hero-text h1 {
        font-size: 3.8rem;
        font-weight: 800;
        line-height: 1.1;
        margin-bottom: 24px;
        letter-spacing: -0.03em;
        color: var(--text-light);
    }
    .hero-text h1 span {
        background: linear-gradient(135deg, #38bdf8 0%, #818cf8 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        display: inline-block;
    }
    .lead { 
        font-size: 1.25rem; 
        color: var(--text-muted); 
        line-height: 1.6; 
        margin-bottom: 40px; 
        max-width: 90%; 
        font-weight: 400;
    }
    
    .hero-actions { display: flex; gap: 16px; margin-bottom: 40px; flex-wrap: wrap; }
    
    .nx-btn {
        padding: 14px 32px; 
        border-radius: 999px; 
        font-weight: 600; 
        font-size: 1rem;
        text-decoration: none; 
        transition: var(--transition-smooth); 
        display: inline-flex; 
        align-items: center; 
        justify-content: center;
        cursor: pointer;
    }
    .nx-btn.primary {
        background: linear-gradient(135deg, #0ea5e9 0%, #3b82f6 100%);
        color: white; 
        box-shadow: 0 4px 20px rgba(14, 165, 233, 0.3);
        border: 1px solid transparent;
    }
    .nx-btn.primary:hover { 
        transform: translateY(-3px); 
        box-shadow: 0 8px 30px rgba(14, 165, 233, 0.5); 
    }
    .nx-btn.outline {
        background: transparent; 
        border: 1px solid var(--glass-border); 
        color: var(--text-light);
    }
    .nx-btn.outline:hover { 
        background: var(--glass-bg); 
        border-color: #38bdf8; 
    }
    
    .hero-badge {
        display: inline-flex; 
        align-items: center;
        padding: 8px 20px; 
        background: rgba(56, 189, 248, 0.05);
        border: 1px solid rgba(56, 189, 248, 0.2); 
        border-radius: 100px;
        font-size: 0.9rem; 
        color: #bae6fd; 
        font-weight: 500;
        backdrop-filter: blur(10px);
    }

    /* Hero Visual - Premium Floating Panels */
    /* Hero Visual - Premium Floating Panels */
    .hero-visual {
        position: relative;
        height: 500px;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    /* Base style for panels */
    .glass-panel {
        position: absolute;
        background: rgba(15, 23, 42, 0.4);
        border: 1px solid rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(16px);
        -webkit-backdrop-filter: blur(16px);
        border-radius: 24px;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
        background-size: cover;
        background-position: center;
        transition: transform 0.6s ease, opacity 0.6s ease;
    }

    /* Painel 1 - Programador interagindo */
    /* Painel 1 - Design Principal */
    .panel-1 {
        width: 340px;
        height: 300px;
        z-index: 2;
        background-image: url("/img/img2.png"); /* Caminho corrigido */
        background-size: cover;
        border-top: 1px solid rgba(56, 189, 248, 0.3);
        animation: float 6s ease-in-out infinite;
    }

    /* Painel 2 - Código renderizando */
    .panel-2 {
        width: 340px;
        height: 300px;
        right: 0;
        top: 20px;
        z-index: 1;
        background-image: url("/img/img3.png"); /* Caminho corrigido */
        background-size: cover;
        border: 1px solid rgba(129, 140, 248, 0.2);
        animation: float 7s ease-in-out infinite reverse;
    }

    /* Painel 3 - Site final no monitor */
    .panel-3 {
        width: 340px;
        height: 300px;
        left: 0;
        bottom: 4px;
        z-index: 3;
        background-image: url("/img/img1.png"); /* Caminho corrigido */
        background-size: cover;
        border: 1px solid rgba(56, 189, 248, 0.4);
        box-shadow: 0 0 400px rgba(56, 189, 248, 0.2);
        animation: float 5s ease-in-out infinite 1s;
    }
    /* Floating animation */
    @@keyframes float {
        0% {
            transform: translateY(0px);
        }

        50% {
            transform: translateY(-20px);
        }

        100% {
            transform: translateY(0px);
        }
    }

    /* Responsividade */
   @@media(max - width: 1024px)

    {
        .hero-grid {
            grid-template-columns: 1fr;
            text-align: center;
            gap: 40px;
        }

        .hero-actions {
            justify-content: center;
        }

        .hero-visual {
            height: 400px;
        }

        .grid-4, .grid-3, .grid-6, .testimonial-grid {
            grid-template-columns: repeat(2, 1fr);
        }
    }


    /* Sessões Genéricas */
    .section-title { font-size: 2.8rem; text-align: center; margin-bottom: 16px; font-weight: 800; letter-spacing: -0.02em; }
    .section-subtitle { text-align: center; color: var(--text-muted); font-size: 1.15rem; margin-bottom: 60px; font-weight: 400; max-width: 600px; margin-inline: auto; }

    /* Cards - Glassmorphism Refinado */
    .grid-4 { display: grid; grid-template-columns: repeat(4, 1fr); gap: 24px; }
    .grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 32px; }
    
    .nx-card, .service-card, .testimonial-card {
        background: rgba(15, 23, 42, 0.4);
        border: 1px solid var(--glass-border);
        padding: 40px 32px;
        border-radius: 20px;
        transition: var(--transition-smooth);
        text-decoration: none;
        color: var(--text-light);
        position: relative;
        overflow: hidden;
    }
    
    .nx-card::before, .service-card::before {
        content: ''; position: absolute; top: 0; left: 0; width: 100%; height: 100%;
        background: radial-gradient(800px circle at var(--mouse-x, 50%) var(--mouse-y, 50%), rgba(56, 189, 248, 0.06), transparent 40%);
        opacity: 0; transition: opacity 0.3s; pointer-events: none;
    }
    
    .nx-card:hover, .service-card:hover {
        transform: translateY(-8px);
        border-color: rgba(56, 189, 248, 0.3);
        background: rgba(30, 41, 59, 0.6);
        box-shadow: 0 20px 40px rgba(0,0,0,0.4), 0 0 20px rgba(56, 189, 248, 0.05);
    }
    .nx-card:hover::before, .service-card:hover::before { opacity: 1; }

    .nx-card h3, .service-card h3 { font-size: 1.3rem; margin-bottom: 16px; font-weight: 700; color: #f8fafc; }
    .nx-card p, .service-card p { color: var(--text-muted); line-height: 1.6; margin: 0; font-size: 0.95rem; }

    /* KPIs */
    .nx-kpis { background: linear-gradient(180deg, transparent, rgba(15, 23, 42, 0.8), transparent); }
    .grid-6 { display: grid; grid-template-columns: repeat(6, 1fr); gap: 24px; text-align: center; }
    .kpi { padding: 24px 0; }
    .kpi .num {
        display: block; font-size: 3.5rem; font-weight: 800;
        background: linear-gradient(135deg, #ffffff 0%, #cbd5e1 100%);
        -webkit-background-clip: text; -webkit-text-fill-color: transparent; margin-bottom: 8px;
        line-height: 1;
    }
    .kpi p { color: #38bdf8; font-weight: 600; font-size: 0.85rem; text-transform: uppercase; letter-spacing: 1.5px; }

    /* Depoimentos */
    .testimonial-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 32px; }
    .stars { color: #fbbf24; margin-bottom: 20px; font-size: 1.2rem; letter-spacing: 2px; }
    .testimonial-card p { font-style: italic; margin-bottom: 24px; font-size: 1.05rem; line-height: 1.7; color: #e2e8f0; }
    .testimonial-card span { display: block; font-size: 0.95rem; font-weight: 700; color: #38bdf8; border-top: 1px solid var(--glass-border); padding-top: 16px; }

    /* CTA Final */
    .nx-final-cta { text-align: center; margin-top: 40px; }
    .cta-box {
        background: radial-gradient(circle at center, rgba(30, 41, 59, 0.8) 0%, rgba(15, 23, 42, 0.9) 100%);
        border: 1px solid rgba(56, 189, 248, 0.2);
        border-radius: 32px;
        padding: 80px 40px;
        box-shadow: 0 20px 60px rgba(0,0,0,0.5);
    }
    .cta-box h2 { font-size: 3rem; margin-bottom: 24px; font-weight: 800; letter-spacing: -0.02em; }
    .cta-box p { font-size: 1.25rem; color: var(--text-muted); margin-bottom: 40px; max-width: 600px; margin-inline: auto; }
    .nx-btn.big { padding: 18px 48px; font-size: 1.15rem; }

    /* Classes de Animação de Scroll (Reveal) */
    .reveal { opacity: 0; transform: translateY(40px); transition: all 0.8s cubic-bezier(0.5, 0, 0, 1); }
    .reveal.active { opacity: 1; transform: translateY(0); }
    .delay-1 { transition-delay: 0.1s; }
    .delay-2 { transition-delay: 0.2s; }
    .delay-3 { transition-delay: 0.3s; }

    /* Responsividade */
    @@media(max-width: 1024px) {
      /*   .hero-grid { grid-template-columns: 1fr; text-align: center; gap: 40px; }
        .hero-actions { justify-content: center; }
        .hero-visual { height: 400px; } */
        .grid-4, .grid-3, .grid-6, .testimonial-grid { grid-template-columns: repeat(2, 1fr); }
    }
    @@media(max-width: 768px) {
        .grid-4, .grid-3, .grid-6, .testimonial-grid { grid-template-columns: 1fr; }
        .hero-text h1 { font-size: 2.8rem; }
        .nx-section { padding: 80px 0; }
        .section-title { font-size: 2.2rem; }
        .cta-box { padding: 40px 20px; }
        .cta-box h2 { font-size: 2.2rem; }
    }
</style>

<section class="nx-section nx-hero">
    <div class="nx-container hero-grid">
        <div class="hero-text reveal">
            <div class="hero-badge" style="margin-bottom: 24px;  margin-top: 140px;">
                ⚡ Transformação digital de ponta a ponta
            </div>
            <h1>Soluções em Software <span>Arquitetadas para Escalar</span></h1>
            <p class="lead">
                Desenvolvemos sistemas corporativos, automações e plataformas web sob medida. Reduza custos operacionais e eleve a eficiência da sua empresa com tecnologia de alto nível.
            </p>
            <div class="hero-actions">
                <a class="nx-btn primary" href="https://wa.me/" target="_blank">
                    Falar com Especialista
                </a>
                <a class="nx-btn outline" href="/Company/Contact">
                    Explorar Nossas Soluções
                </a>
            </div>
        </div>

        <div class="hero-visual reveal delay-2">
            <div class="glass-panel panel-1"></div>
            <div class="glass-panel panel-2"></div>
            <div class="glass-panel panel-3"></div>
        </div>

    </div>
</section>

<section class="nx-section nx-benefits">
    <div class="nx-container grid-4">
        <div class="nx-card reveal">
            <h3>📊 Controle Total</h3>
            <p>Sistemas personalizados projetados especificamente para a realidade e complexidade da sua operação.</p>
        </div>
        <div class="nx-card reveal delay-1">
            <h3>⚙️ Automação Inteligente</h3>
            <p>Elimine tarefas manuais repetitivas, minimize erros humanos e ganhe velocidade de execução.</p>
        </div>
        <div class="nx-card reveal delay-2">
            <h3>🚀 Performance Extrema</h3>
            <p>Aplicações com arquitetura moderna, ultrarrápidas e preparadas para alto volume de acessos.</p>
        </div>
        <div class="nx-card reveal delay-3">
            <h3>🧩 Integração Simples</h3>
            <p>Conectamos seu novo software às ferramentas que você já usa através de APIs robustas e seguras.</p>
        </div>
    </div>
</section>

<section class="nx-section nx-services">
    <div class="nx-container">
        <h2 class="section-title reveal">Nossa Expertise Técnica</h2>
        <p class="section-subtitle reveal">Engenharia de software focada em resolver gargalos reais de negócios complexos.</p>

        <div class="grid-3">
            <a class="service-card reveal" href="/Services/Software">
                <h3>💻 Software Sob Medida</h3>
                <p>Arquitetura e desenvolvimento de sistemas internos, ERPs customizados, painéis de gestão de dados e plataformas SaaS escaláveis.</p>
            </a>
            <a class="service-card reveal delay-1" href="/Services/WebApps">
                <h3>🌐 Web Apps & Portais</h3>
                <p>Presença digital premium com interfaces responsivas, design focado em UX/UI e aplicações progressivas otimizadas para conversão.</p>
            </a>
            <a class="service-card reveal delay-2" href="/Services/Automations">
                <h3>🤖 Automações & APIs</h3>
                <p>Criação de robôs de atendimento integrados ao WhatsApp, orquestração de microsserviços e automação completa de fluxos de trabalho.</p>
            </a>
        </div>
    </div>
</section>

<section class="nx-section nx-kpis">
    <div class="nx-container">
        <div class="grid-6 counters">
            <div class="kpi reveal">
                <span class="num" data-target="128">0</span>
                <p>Projetos Entregues</p>
            </div>
            <div class="kpi reveal delay-1">
                <span class="num" data-target="98" data-suffix="%">0</span>
                <p>Taxa de Retenção</p>
            </div>
            <div class="kpi reveal delay-2">
                <span class="num" data-target="45000" data-suffix="+">0</span>
                <p>Transações/Dia</p>
            </div>
            <div class="kpi reveal delay-3">
                <span class="num" data-target="24" data-suffix="/7">0</span>
                <p>Suporte Técnico</p>
            </div>
            <div class="kpi reveal delay-1">
                <span class="num" data-target="30" data-suffix="ms">0</span>
                <p>Latência Média</p>
            </div>
            <div class="kpi reveal delay-2">
                <span class="num" data-target="99" data-suffix="%">0</span>
                <p>Uptime Garantido</p>
            </div>
        </div>
    </div>
</section>

<section class="nx-section nx-testimonials">
    <div class="nx-container">
        <h2 class="section-title reveal">Quem confia na NexCore</h2>
        <p class="section-subtitle reveal">Resultados reais medidos pelos líderes que utilizam nossa tecnologia.</p>

        <div class="testimonial-grid">
            <div class="testimonial-card reveal">
                <div class="stars">★★★★★</div>
                <p>"A arquitetura do novo sistema reduziu em 40% nosso tempo operacional. A estabilidade da plataforma mesmo em picos de acesso é impressionante."</p>
                <span>Carla Mendes — Diretora de Operações</span>
            </div>
            <div class="testimonial-card reveal delay-1">
                <div class="stars">★★★★★</div>
                <p>"A integração de APIs que a NexCore construiu entre nosso ERP e o WhatsApp praticamente zerou nossos erros de faturamento."</p>
                <span>João Pedro — Head de Tecnologia</span>
            </div>
            <div class="testimonial-card reveal delay-2">
                <div class="stars">★★★★★</div>
                <p>"Código limpo, entregas no prazo e um time técnico que realmente entende de regras de negócios complexas. Recomendo de olhos fechados."</p>
                <span>Ana Beatriz — CEO, TechLogistics</span>
            </div>
        </div>
    </div>
</section>

<section class="nx-section nx-final-cta reveal">
    <div class="nx-container">
        <div class="cta-box">
            <h2>Pronto para escalar com a tecnologia certa?</h2>
            <p>Agende uma reunião técnica com nossos arquitetos de software e descubra como podemos acelerar o crescimento da sua operação.</p>
            <a class="nx-btn primary big" href="/Company/Contact">
                Solicitar Diagnóstico Técnico Integrado
            </a>
        </div>
    </div>
</section>

<script>
    document.addEventListener("DOMContentLoaded", () => {
        
        // --- 1. Animação de Surgimento (Reveal no Scroll) ---
        const reveals = document.querySelectorAll('.reveal');
        const revealObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('active');
                    revealObserver.unobserve(entry.target); // Anima só uma vez
                }
            });
        }, { threshold: 0.15, rootMargin: "0px 0px -50px 0px" });

        reveals.forEach(reveal => revealObserver.observe(reveal));

        // --- 2. Contador de KPIs (Suave e com formatação pt-BR) ---
        const counters = document.querySelectorAll(".num");
        const counterObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if(entry.isIntersecting) {
                    const counter = entry.target;
                    const target = +counter.getAttribute("data-target");
                    const suffix = counter.getAttribute("data-suffix") || "";
                    const duration = 2500; // 2.5 segundos de animação
                    let startTimestamp = null;

                    const step = (timestamp) => {
                        if (!startTimestamp) startTimestamp = timestamp;
                        const progress = Math.min((timestamp - startTimestamp) / duration, 1);
                        
                        // Easing (easeOutExpo) para desacelerar no final
                        const easeProgress = progress === 1 ? 1 : 1 - Math.pow(2, -10 * progress);
                        const current = Math.floor(easeProgress * target);
                        
                        // Formata o número (ex: 45000 -> 45.000)
                        counter.innerText = current.toLocaleString('pt-BR') + suffix;

                        if (progress < 1) {
                            window.requestAnimationFrame(step);
                        } else {
                            counter.innerText = target.toLocaleString('pt-BR') + suffix; // Garante o final exato
                        }
                    };
                    window.requestAnimationFrame(step);
                    counterObserver.unobserve(counter);
                }
            });
        }, { threshold: 0.5 });

        counters.forEach(counter => counterObserver.observe(counter));
    });
</script> *@

@inject Microsoft.Extensions.Options.IOptions<AppOptions> App

<style>
    /* Estilos Premium Reutilizados */
    .nx-section {
        padding: 120px 0;
        position: relative;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .nx-container {
        max-width: 1280px;
        margin: 0 auto;
        padding: 0 24px;
        z-index: 2;
        position: relative;
    }

    /* Background e Efeitos */
    body {
        background-color: #0f172a;
        margin: 0;
        overflow-x: hidden;
    }

    .bg-gradient {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: radial-gradient(circle at 50% 50%, rgba(56, 189, 248, 0.1) 0%, transparent 70%);
        pointer-events: none;
        z-index: 1;
    }

    /* Hero Centralizado para Atualização */
    .hero-maintenance {
        text-align: center;
        max-width: 800px;
        margin: 0 auto;
    }

        .hero-maintenance h1 {
            font-size: 4rem;
            font-weight: 800;
            line-height: 1.1;
            margin-bottom: 24px;
            letter-spacing: -0.03em;
            color: var(--text-light, #f8fafc);
        }

            .hero-maintenance h1 span {
                background: linear-gradient(135deg, #38bdf8 0%, #818cf8 100%);
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
            }

    .lead-maintenance {
        font-size: 1.5rem;
        color: #94a3b8;
        line-height: 1.6;
        margin-bottom: 40px;
    }

    /* Badge de Status Animado */
    .status-badge {
        display: inline-flex;
        align-items: center;
        padding: 8px 20px;
        background: rgba(56, 189, 248, 0.1);
        border: 1px solid rgba(56, 189, 248, 0.3);
        border-radius: 100px;
        font-size: 0.9rem;
        color: #38bdf8;
        font-weight: 600;
        margin-bottom: 30px;
        animation: pulse 2s infinite;
    }

    @@keyframes pulse {
        0% {
            opacity: 0.6;
        }

        50% {
            opacity: 1;
        }

        100% {
            opacity: 0.6;
        }
    }

    /* Botões */
    .hero-actions {
        display: flex;
        gap: 16px;
        justify-content: center;
    }

    .nx-btn {
        padding: 14px 32px;
        border-radius: 999px;
        font-weight: 600;
        font-size: 1rem;
        text-decoration: none;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
    }

        .nx-btn.primary {
            background: linear-gradient(135deg, #0ea5e9 0%, #3b82f6 100%);
            color: white;
            box-shadow: 0 4px 20px rgba(14, 165, 233, 0.3);
        }

            .nx-btn.primary:hover {
                transform: translateY(-3px);
                box-shadow: 0 8px 30px rgba(14, 165, 233, 0.5);
            }

    /* Floating Visual Panels (Mantidos do seu original) */
    .hero-visual-maintenance {
        position: relative;
        height: 300px;
        margin-top: 60px;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .glass-panel {
        position: absolute;
        background: rgba(15, 23, 42, 0.4);
        border: 1px solid rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(16px);
        border-radius: 24px;
        width: 280px;
        height: 180px;
        background-size: cover;
        background-position: center;
        animation: float 6s ease-in-out infinite;
    }

    .panel-1 {
        background-image: url("/img/img1.png");
        left: 10%;
        transform: rotate(-5deg);
    }

    .panel-2 {
        background-image: url("/img/img2.png");
        right: 20%;
        animation-delay: 1s;
        transform: rotate(5deg);
    }

    @@keyframes float {
        0%, 100% {
            transform: translateY(0px) rotate(-5deg);
        }

        50% {
            transform: translateY(-20px) rotate(-2deg);
        }
    }

    /* Reveal Animation */
    .reveal {
        opacity: 0;
        transform: translateY(30px);
        transition: all 1s ease;
    }

        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }
</style>

<div class="bg-gradient"></div>

<section class="nx-section">
    <div class="nx-container">
        <div class="hero-maintenance reveal active">
            <div class="status-badge">
                🛠️ ESTAMOS EM ATUALIZAÇÃO
            </div>
            <h1>NexCore <span>Elevando o Nível</span></h1>
            <p class="lead-maintenance">
                Estamos arquitetando uma nova experiência digital. Em breve, você terá acesso a soluções em software ainda mais robustas e escaláveis.
            </p>

            <div class="hero-actions">
                <a class="nx-btn primary" href="https://wa.me/SEUNUMERO" target="_blank">
                    Falar via WhatsApp
                </a>
            </div>

            <div class="hero-visual-maintenance reveal active" style="transition-delay: 0.5s">
                <div class="glass-panel panel-1"></div>
                <div class="glass-panel panel-2"></div>
            </div>
        </div>
    </div>
</section>

<script>
    document.addEventListener("DOMContentLoaded", () => {
        const reveals = document.querySelectorAll('.reveal');
        setTimeout(() => {
            reveals.forEach(el => el.classList.add('active'));
        }, 100);
    });
</script>